global:
  resolve_timeout: 5m

route:
  receiver: "local-alerts"
  group_by: ["alertname", "service"]
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 5m

receivers:
  - name: "local-alerts"
